## Инструкция по сборке и запуску

Для компиляции проекта вам понадобятся компилятор с поддержкой **C++17** (или выше) и утилита **CMake**.

### Запуск на Linux (Ubuntu/Debian/Arch)

1.  Откройте терминал и перейдите в корень проекта:
    ```bash
    cd /home/frost/projects/rpg-game
    ```
2.  Создайте папку для сборки и перейдите в неё:
    ```bash
    mkdir build && cd build
    ```
3.  Сконфигурируйте проект с помощью CMake (он автоматически подготовит `map.json` в папке сборки):
    ```bash
    cmake ..
    ```
4.  Скомпилируйте проект:
    ```bash
    make
    ```
5.  Запустите игру:
    ```bash
    ./rpg_game
    ```

### Запуск на Windows (Command Prompt / PowerShell)

#### Способ 1: Через MinGW (GCC) в консоли
1.  Откройте командную строку (CMD) и перейдите в папку проекта:
    ```cmd
    cd C:\projects\rpg-game
    ```
2.  Создайте папку сборки:
    ```cmd
    mkdir build
    cd build
    ```
3.  Сгенерируйте файлы сборки для MinGW:
    ```cmd
    cmake -G "MinGW Makefiles" ..
    ```
4.  Скомпилируйте проект:
    ```cmd
    cmake --build .
    ```
5.  Запустите игру:
    ```cmd
    rpg_game.exe
    ```

#### Способ 2: Через Visual Studio
1.  Откройте **Visual Studio**.
2.  Выберите пункт **"Открыть локальную папку"** (Open a local folder) и укажите корень вашего проекта `rpg-game`.
3.  Visual Studio автоматически распознает файл `CMakeLists.txt` и настроит проект.
4.  Нажмите кнопку **"Уведомление о запуске / Выбрать целевой объект"** (Зелёная стрелочка сверху) и выберите `rpg_game.exe`. Проект скомпилируется и запустится сам.

---

## Структура Репозитория

```text
├── CMakeLists.txt        # Сценарий сборки CMake (настроен для Windows и Linux)
├── map.json              # База данных локаций игрового мира в формате JSON
├── README.md             # Документация проекта
├── headers/              # Заголовочные файлы (.hpp)
│   ├── json.hpp          # Библиотека nlohmann/json для парсинга файлов
│   ├── map.hpp           # Объявления карты и переходов
│   ├── player.hpp        # Структура персонажа, объявления Save/Load системы и респавна
│   ├── utils.hpp         # Общие кроссплатформенные утилиты и структура Monster
│   ├── battle.hpp        # Объявление интерфейса пошаговой боевой системы
│   ├── npc.hpp           # Менеджер диалогов и объявления подфункций персонажей
│   └── story_mira.hpp    # Интерфейс сюжетной ветки Алхимика Миры
└── src/                  # Исполняемый код (.cpp)
    ├── main.cpp          # Главное меню игры, стартовая точка и кинематографичное вступление
    ├── map.cpp           # Логика загрузки JSON, переходов по графу и триггеров опасных зон
    ├── player.cpp        # Реализация статов, безопасной Save/Load системы и лечения в мирных зонам
    ├── battle.cpp        # Реализация пошагового боя с фильтрацией сумки и защитой от читеров
    ├── npc.cpp           # Логика торговцев, квестов и обычных жителей (Гром, Гарри, Карим и др.)
    └── story_mira.cpp    # Реализация масштабного сюжета Алхимика Миры и финальных битв

```

#### Отдельная благодарность моему товарищу Алексею за помощь с тестами на Windows
